---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: myweb-prom
  labels:
    app: myweb-monitoring
spec:
  type: ClusterIP
  ports:
    - name: http-metrics
      port: 8081
      targetPort: http-metrics
  selector:
    app: {{ .Values.appName }}
